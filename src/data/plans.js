/* ---------------------------------------------------------
   Plan catalog. Every plan here comes from a real affiliate provider
   and links to its own verified detail page — generated by
   `npm run import-plans` (see scripts/import-plans/), which crawls
   each registered provider's full catalog. Don't hand-edit the plans
   below; edit a provider adapter and regenerate instead.
--------------------------------------------------------- */
import generated from "./plans.generated.json";

export const PLANS = generated;

// Derived from whatever styles the providers actually returned, so a
// newly added provider's categories show up as filter options
// automatically — no manual list to keep in sync.
export const STYLES = [...new Set(PLANS.map((p) => p.style))].sort();

export const FEATURES = [
  "porch",
  "open floor plan",
  "primary suite on main",
  "office",
  "basement",
  "vaulted ceilings",
];
